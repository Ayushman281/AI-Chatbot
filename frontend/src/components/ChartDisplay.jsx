import React from 'react';

export default function ChartDisplay({ data, type }) {
    // TODO: Integrate with a chart library
    return (
        <div>
            <i>Chart ({type}) would be rendered here.</i>
        </div>
    );
}
